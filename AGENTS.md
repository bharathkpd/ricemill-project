# Project Guidelines

- Maintain clean, standard React 19 + TanStack Start + Vite architecture.
- Ensure all static assets and images reside in `public/assets/` and are referenced using standard absolute paths (`/assets/filename.jpg`).
- Follow modern TypeScript and styling best practices using Tailwind CSS and Radix UI components.
